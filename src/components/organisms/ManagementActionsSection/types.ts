export type ManagementActionsSectionProps = {
  filterSection?: React.ReactNode;
  searchSection?: React.ReactNode;
  createButton?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
};
